services:
  pgsql:
    image: postgis/postgis:16-3.4-alpine
    ports:
      - 5432:5432
    env_file:
      - ./.env
    container_name: pgsql
    volumes:
      - ./pgsql_volume:/var/lib/postgresql/data
    networks:
      - app

networks:
  app:
    external: true
