version: "3.9"

services:
  pgsql:
    image: postgis/postgis:14-3.3
    ports:
      - 5432:5432
    env_file:
      - ./.env
    container_name: pgsql
    volumes:
      - ./data:/var/lib/postgresql/data
    
