version: "3.9"

services:
  pgsql:
    image: postgis/postgis:14-3.2-alpine
    ports:
      - 5432:5432
    env_file:
      - ./.env
    container_name: pgsql
    volumes:
      - ./pgsql_volume:/var/lib/postgresql/data
    networks:
      - app

networks:
  app:
    external: true
