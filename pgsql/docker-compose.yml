
services:
  pgsql:
    image: postgis/postgis:18-3.6
    container_name: pgsql
    platform: linux/amd64
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql
